<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Finger_Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:label">指纹识别</item>
        <item name="drawerArrowStyle">@style/Finger_Theme.DrawerArrowToggle</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Finger_Theme.DrawerArrowToggle" parent="Base.Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@android:color/white</item>
        <item name="windowNoTitle">true</item>
    </style>

    //去除状态栏
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="EditTextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">24dp</item>
        <item name="android:background">@null</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColor">@android:color/background_dark</item>
    </style>

</resources>
